/**
 * Shared DOMAIN layer — pure Java business rules reused across features.
 * <p>
 * NO Spring, NO JPA, NO web. (e.g. base DomainException types, shared value objects.)
 */
package com.cuea.rmp.common.domain;
